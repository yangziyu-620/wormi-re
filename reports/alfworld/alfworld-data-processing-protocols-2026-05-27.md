# ALFWorld Data Processing Protocols

Date: 2026-05-27

This report records the two ALFWorld processing protocols generated by `tools/build_alfworld_dataset.py`, the reasoning behind them, and the validation status of the generated releases.

## Shared Principles

- Start from official ALFWorld textual-env expert episodes.
- Build canonical episode files first, then derive method-specific views.
- Keep every method's zero-shot training union equal to `splits/train.jsonl`.
- Exclude unseen-task episodes from all zero-shot training views.
- Evaluate all methods on the same `eval_col_*` episode files.
- Use scene-domain WorMI world-model clusters instead of task-type experts, so unseen tasks do not leak into world-model training.

## Generated Releases

### `paper-compatible-v1`

ALFWorld Paper-Compatible Room-Type Protocol v1

Rationale:

- The paper states 4 scene types with 3 seen and 1 unseen, but does not publish the held-out scene type or the two held-out task types.
- Kitchens are selected as the unseen scene type because heat and cool tasks are physically kitchen-bound; this makes col_3 non-empty.
- The seen-task unseen-scene column still contains multiple tasks (simple, clean, pick-two), avoiding the bathrooms split where col_2 collapses to only pick_and_place_simple.
- Unseen-task episodes are excluded from all zero-shot training views; WorMI world models are scene-clustered from the same seen-domain pool instead of being trained as task-type experts.

Validation summary:

- valid: `True`
- errors: `0`
- warnings: `0`
- split counts: `{'eval_col_1_seen_seen': 186, 'eval_col_2_seen_unseen': 704, 'eval_col_3_unseen_unseen': 992, 'monitor': 186, 'train': 1485, 'unused_unseen_task_seen_scene': 0}`
- col2 tasks: `{'pick_and_place_simple': 79, 'pick_clean_then_place_in_recep': 501, 'pick_two_obj_and_place': 124}`
- col3 tasks: `{'pick_cool_then_place_in_recep': 533, 'pick_heat_then_place_in_recep': 459}`
- output: `/root/autodl-tmp/wormi-data/alfworld-protocols/paper-compatible-v1`

### `balanced-scene-instance-v1`

ALFWorld Balanced Scene-Instance Protocol v1

Rationale:

- This protocol is for fair method comparison, not direct paper-table reproduction.
- The scene split is by ALFRED scene number within every room type, so unseen-scene evaluation is not reduced to a single room's physical task affordances.
- Pick-two and clean are held out as compositional unseen tasks while atomic pick-and-place remains a seen-task baseline.
- Unseen-task episodes are excluded from all zero-shot training views; WorMI world models are scene-clustered from the same seen-domain pool.

Validation summary:

- valid: `True`
- errors: `0`
- warnings: `0`
- split counts: `{'eval_col_1_seen_seen': 161, 'eval_col_2_seen_unseen': 479, 'eval_col_3_unseen_unseen': 336, 'monitor': 161, 'train': 1289, 'unused_unseen_task_seen_scene': 1127}`
- col2 tasks: `{'look_at_obj_in_light': 97, 'pick_and_place_simple': 175, 'pick_cool_then_place_in_recep': 121, 'pick_heat_then_place_in_recep': 86}`
- col3 tasks: `{'pick_clean_then_place_in_recep': 158, 'pick_two_obj_and_place': 178}`
- output: `/root/autodl-tmp/wormi-data/alfworld-protocols/balanced-scene-instance-v1`
